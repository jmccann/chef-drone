#!/bin/bash

# server configuration

SERVER_ADDR="<%= node['drone']['server_address'] %>"
SERVER_CERT="<%= node['drone']['server_crt'] %>"
SERVER_KEY="<%= node['drone']['server_key'] %>"

<% node['drone']['config'].each do |key, value| -%>
<%= key.upcase %>="<%= value %>"
<% end -%>
