start on (filesystem and net-device-up)

# this should be a path where your sqlite resides
chdir /var/lib/drone
console log

script
	set -a
	if [ -f /etc/drone/dronerc ]; then
		. /etc/drone/dronerc
	fi
    /usr/local/bin/drone <%= node['drone']['service_options'] %>
end script

